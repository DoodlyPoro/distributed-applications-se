@page
@model FrontEnd.Pages.Sequences.CreateModel

@{
    ViewData["Title"] = "Create Sequence";
}

<div class="container mt-5" style="max-width: 650px;">
    <div class="card shadow border-0 rounded-4">
        <div class="card-body p-4">

            <h2 class="fw-bold mb-2">Create Sequence</h2>
            <p class="text-muted mb-4">Add a new sequence.</p>

            @if (!string.IsNullOrEmpty(Model.Error))
            {
                <div class="alert alert-danger">@Model.Error</div>
            }

            <form method="post">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label asp-for="Sequence.Number" class="form-label"></label>
                    <input asp-for="Sequence.Number" class="form-control" />
                    <span asp-validation-for="Sequence.Number" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Sequence.Name" class="form-label"></label>
                    <input asp-for="Sequence.Name" class="form-control" />
                    <span asp-validation-for="Sequence.Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Sequence.Description" class="form-label"></label>
                    <textarea asp-for="Sequence.Description" class="form-control" rows="4"></textarea>
                    <span asp-validation-for="Sequence.Description" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Sequence.PathwayId" class="form-label">Pathway ID</label>
                    <input asp-for="Sequence.PathwayId" class="form-control" placeholder="Optional" />
                    <span asp-validation-for="Sequence.PathwayId" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-page="/Sequences/Index" class="btn btn-outline-secondary">Cancel</a>
                    <button type="submit" class="btn btn-dark">Create</button>
                </div>
            </form>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}